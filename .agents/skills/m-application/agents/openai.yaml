interface:
  display_name: "M Application"
  short_description: "Repo-specific patterns and workflows for m-application"
  default_prompt: "Use the m-application repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true